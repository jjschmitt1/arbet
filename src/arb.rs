pub enum RetVals{
    ArbAvail,
    NoArb,
}

pub fn calc_arb(line_one: &i32, line_two: &i32) -> Result<RetVals, String> {

    // calculate arbitrage opportunities here

    Err("Error calculating Arbitrage.".to_string())
}